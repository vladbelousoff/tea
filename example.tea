@export
fn fibonacci(n: i32) {
}

fn calculate(value: i32, multiplier: i32) {
}

fn hello() {
}

@inline
fn process(data: string, count: i32, flag: bool) {
}

let mut x = 100;
let y = 2.5;
x = x - 10;

let value = 5 - x - (y * -3.5 + 10);
let mut z = 0;

z = 777;
z = x * value / 24.3 - 900;
